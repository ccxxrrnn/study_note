server:
  port: 80


eureka:
  client:
    register-with-eureka: false
    service-url:
      #      defaultZone: http://localhost:7001/eureka
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka #集群版

ribbon:
  #建立连接所用的时间
  ConnectTimeout: 5000
  #建立连接后读取用的时间
  ReadTimeout: 5000
logging:
  level:
    com.xw.spring.service.PaymentFeignService: debug
